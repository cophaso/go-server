CREATE TABLE comments(
  -- id INTEGER PRIMARY KEY GENERATED BY DEFAULT BY IDENTITY,
  id SERIAL PRIMARY KEY,
  description TEXT, 
  activity_item_id INTEGER REFERENCES activity_items(id) ON DELETE CASCADE NOT NULL
);